fun probarResistenciaDelParser (x : entero; y : real; z : valor) dev (resultado : real) =
    sea a = x + y ^ 3 * -5 / c (x, y), b = caso x > 0 -> y [] x <= 0 -> -y fcaso en
        caso b -> sea temporal = a en temporal
           [] a > b -> x ^ (y + 1)
           [] a == b -> caso caso a -> b fcaso -> caso b -> a fcaso fcaso
        fcaso
ffun